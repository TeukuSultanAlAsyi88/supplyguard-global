@extends('layouts.app') @section('title','Kelola Pengguna') @section('heading','Kelola Pengguna') @section('content')<div class="mb-4"><h2 class="page-title">Data Pengguna</h2><p class="text-muted">Atur peran dan status akun.</p></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Nama</th><th>Email</th><th>Peran & Status</th><th>Dibuat</th><th>Aksi</th></tr></thead><tbody>@foreach($users as $u)<tr><td><strong>{{ $u->name }}</strong></td><td>{{ $u->email }}</td><td><form class="d-flex gap-2 align-items-center" method="POST" action="{{ route('admin.users.update',$u) }}">@csrf @method('PUT')<select class="form-select form-select-sm" name="role" style="width:110px"><option value="user" @selected($u->role==='user')>Pengguna</option><option value="admin" @selected($u->role==='admin')>Admin</option></select><div class="form-check"><input type="hidden" name="is_active" value="0"><input class="form-check-input" type="checkbox" name="is_active" value="1" @checked($u->is_active)><label class="form-check-label">Aktif</label></div><button class="btn btn-sm btn-primary">Simpan</button></form></td><td>{{ $u->created_at->format('d M Y') }}</td><td>@if($u->id!==auth()->id())<form method="POST" action="{{ route('admin.users.destroy',$u) }}" onsubmit="return confirm('Hapus pengguna ini?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Hapus</button></form>@endif</td></tr>@endforeach</tbody></table></div><div class="card-footer bg-white">{{ $users->links() }}</div></div>@endsection